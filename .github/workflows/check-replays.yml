name: Check Replays

permissions:
  contents: read
  pull-requests: write

on:
  workflow_call:
    inputs:
      game:
        required: true
        type: string
        description: "Game to check (only GeneralsMD for now)"
      userdata:
        required: true
        type: string
        description: "Path to folder with replays and maps"
      preset:
        required: true
        type: string
        description: "CMake preset"

jobs:
  gamedata:
    # The replay run needs retail game data, which is not redistributable and so is not in the
    # repository. A checkout without access to a copy of it cannot run this gate at all, and a
    # gate that fails for a missing input says nothing about the code. Decide up front whether
    # the data is reachable and skip the run with a reason if it is not, so that setting the
    # variables and secrets later re-enables the gate with no change to this file.
    name: Game data available?
    runs-on: ubuntu-latest
    outputs:
      available: ${{ steps.probe.outputs.available }}
    steps:
      - name: Probe Game Data Configuration
        id: probe
        env:
          ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          if [ -n "$ACCESS_KEY_ID" ] && [ -n "$SECRET_ACCESS_KEY" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
            echo "Bucket credentials are set; running the replay check." >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          echo "available=false" >> "$GITHUB_OUTPUT"
          {
            echo "### Replay check skipped: no game data"
            echo
            echo "\`R2_ACCESS_KEY_ID\` and \`R2_SECRET_ACCESS_KEY\` are not both set in this"
            echo "repository, so there is nowhere to download the retail game data from and the"
            echo "replay determinism gate cannot run. It is skipped rather than failed, because a"
            echo "failure here would say nothing about the code under test."
            echo
            echo "**Replay determinism is therefore unverified on this pull request.** Run the"
            echo "check locally with \`scripts/ci/run-replays-local.ps1\`, or configure hosted game"
            echo "data to get the gate back — both are documented in"
            echo "\`docs/porting/replay-check-gamedata.md\`."
          } >> "$GITHUB_STEP_SUMMARY"

  build:
    name: ${{ inputs.preset }}
    needs: gamedata
    if: ${{ needs.gamedata.outputs.available == 'true' }}
    runs-on: windows-2022
    timeout-minutes: 15
    env:
      GAME_PATH: C:\GameData
      GENERALS_PATH: C:\GameData\Generals
      GENERALSMD_PATH: C:\GameData\GeneralsMD
      # Where the trimmed game data lives and what it should hash to. Override per repository
      # (Settings -> Secrets and variables -> Actions -> Variables) to use your own bucket; the
      # defaults are the upstream bucket and the hashes of the archives in it.
      GAMEDATA_S3_BASE_URI: ${{ vars.GAMEDATA_S3_BASE_URI || 's3://github-ci' }}
      EXPECTED_HASH_GENERALS: ${{ vars.GAMEDATA_GENERALS_SHA256 || '37A351AA430199D1F05DEB9E404857DCE7B461A6AC272C5D4A0B5652CDB06372' }}
      EXPECTED_HASH_GENERALSMD: ${{ vars.GAMEDATA_GENERALSMD_SHA256 || '6837FE1E3009A4C239406C39B1598216C0943EE8ED46BB10626767029AC05E21' }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          submodules: true

      - name: Download Game Artifact
        uses: actions/download-artifact@70fc10c6e5e1ce46ad2ea6f2b72d43f7d47b13c3 # v8.0.0
        with:
          name: ${{ inputs.game }}-${{ inputs.preset }}
          path: build

      - name: Cache Game Data
        id: cache-gamedata
        uses: actions/cache@cdf6c1fa76f9f475f3d7449005a359c84ca0f306 # v5.0.3
        with:
          path: ${{ env.GAME_PATH }}
          # The hashes are part of the key so that repointing the workflow at a differently
          # packed copy of the game data does not silently reuse the previous one.
          key: gamedata-permanent-cache-v4-${{ env.EXPECTED_HASH_GENERALS }}-${{ env.EXPECTED_HASH_GENERALSMD }}

      - name: Download Game Data
        if: ${{ steps.cache-gamedata.outputs.cache-hit != 'true' }}
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_ENDPOINT_URL: ${{ secrets.R2_ENDPOINT_URL }}
        shell: pwsh
        run: |
          # Download trimmed gamedata of both Generals 1.08 and Generals Zero Hour 1.04.
          # This data cannot be used for playing because it's
          # missing textures, audio and gui files. But it's enough for replay checking.
          # It is not redistributable, hence a private bucket and a hash check.
          #
          # The bucket, the endpoint and the expected hashes are all configurable, so a fork
          # that cannot use the upstream bucket can pack its own copy from retail installs with
          # scripts/ci/pack-gamedata.ps1 and host it itself. AWS_ENDPOINT_URL is only needed for
          # S3-compatible services such as Cloudflare R2; leave it unset for plain AWS S3.
          # See docs/porting/replay-check-gamedata.md.

          if (-not $env:AWS_ACCESS_KEY_ID -or -not $env:AWS_SECRET_ACCESS_KEY) {
              $ok1 = [bool]$env:AWS_ACCESS_KEY_ID
              $ok2 = [bool]$env:AWS_SECRET_ACCESS_KEY
              Write-Host "Required bucket credentials are not set or are empty. R2_ACCESS_KEY_ID: $ok1, R2_SECRET_ACCESS_KEY: $ok2"
              Write-Host "See docs/porting/replay-check-gamedata.md for how to provide game data in a fork."
              exit 1
          }

          $awsArgs = @()
          if ($env:AWS_ENDPOINT_URL) { $awsArgs += @("--endpoint-url", $env:AWS_ENDPOINT_URL) }

          # Download Generals Game Files
          # The archive contains these files:
          # BINKW32.DLL
          # English.big
          # INI.big
          # Maps.big
          # mss32.dll
          # W3D.big
          # Data\Scripts\MultiplayerScripts.scb
          # Data\Scripts\SkirmishScripts.scb

          Write-Host "Downloading Game Data for Generals" -ForegroundColor Cyan
          aws s3 cp "$env:GAMEDATA_S3_BASE_URI/generals108_gamedata_trimmed.7z" generals108_gamedata_trimmed.7z @awsArgs
          if ($LASTEXITCODE -ne 0) {
              Write-Error "Download failed (aws exit $LASTEXITCODE). Check GAMEDATA_S3_BASE_URI and the bucket credentials."
              exit 1
          }

          Write-Host "Verifying File Integrity" -ForegroundColor Cyan
          $fileHash = (Get-FileHash -Path generals108_gamedata_trimmed.7z -Algorithm SHA256).Hash
          Write-Host "Downloaded file SHA256: $fileHash"
          Write-Host "Expected file SHA256: $env:EXPECTED_HASH_GENERALS"
          if ($fileHash -ne $env:EXPECTED_HASH_GENERALS) {
              Write-Error "Hash verification failed! File may be corrupted or tampered with."
              exit 1
          }

          Write-Host "Extracting Archive" -ForegroundColor Cyan
          $extractPath = $env:GENERALS_PATH
          & 7z x generals108_gamedata_trimmed.7z -o"$extractPath"
          Remove-Item generals108_gamedata_trimmed.7z -Verbose

          # Download GeneralsMD (ZH) Game Files
          # The archive contains these files:
          # BINKW32.DLL
          # INIZH.big
          # MapsZH.big
          # mss32.dll
          # W3DZH.big
          # Data\Scripts\MultiplayerScripts.scb
          # Data\Scripts\Scripts.ini
          # Data\Scripts\SkirmishScripts.scb

          Write-Host "Downloading Game Data for GeneralsMD" -ForegroundColor Cyan
          aws s3 cp "$env:GAMEDATA_S3_BASE_URI/zerohour104_gamedata_trimmed.7z" zerohour104_gamedata_trimmed.7z @awsArgs
          if ($LASTEXITCODE -ne 0) {
              Write-Error "Download failed (aws exit $LASTEXITCODE). Check GAMEDATA_S3_BASE_URI and the bucket credentials."
              exit 1
          }

          Write-Host "Verifying File Integrity" -ForegroundColor Cyan
          $fileHash = (Get-FileHash -Path zerohour104_gamedata_trimmed.7z -Algorithm SHA256).Hash
          Write-Host "Downloaded file SHA256: $fileHash"
          Write-Host "Expected file SHA256: $env:EXPECTED_HASH_GENERALSMD"
          if ($fileHash -ne $env:EXPECTED_HASH_GENERALSMD) {
              Write-Error "Hash verification failed! File may be corrupted or tampered with."
              exit 1
          }

          Write-Host "Extracting Archive" -ForegroundColor Cyan
          $extractPath = $env:GENERALSMD_PATH
          & 7z x zerohour104_gamedata_trimmed.7z -o"$extractPath"
          Remove-Item zerohour104_gamedata_trimmed.7z -Verbose

      - name: Set Up Game Data
        shell: pwsh
        run: |
          $source = "$env:GAME_PATH\${{ inputs.game }}"
          $destination = "build"
          Copy-Item -Path $source\* -Destination $destination -Recurse -Force

      - name: Set Generals InstallPath in Registry
        shell: pwsh
        run: |
          # Zero Hour loads some Generals files and needs this registry key to find the
          # Generals data files.

          $regPath = "HKCU:\SOFTWARE\Electronic Arts\EA Games\Generals"
          $installPath = "$env:GENERALS_PATH\"

          # Ensure the key exists
          if (-not (Test-Path $regPath)) {
            New-Item -Path $regPath -Force | Out-Null
          }

          # Set the InstallPath value
          Set-ItemProperty -Path $regPath -Name InstallPath -Value $installPath -Type String
          Write-Host "Registry key set: $regPath -> InstallPath = $installPath"

      - name: Move Replays and Maps to User Dir
        shell: pwsh
        run: |
          # These files are expected in the user dir, so we move them here.

          $source = "${{ inputs.userdata }}\Replays"
          $destination = "$env:USERPROFILE\Documents\Command and Conquer Generals Zero Hour Data\Replays"
          Write-Host "Move replays to $destination"
          New-Item -ItemType Directory -Path $destination -Force | Out-Null
          Move-Item -Path "$source\*" -Destination $destination -Force

          $source = "${{ inputs.userdata }}\Maps"
          $destination = "$env:USERPROFILE\Documents\Command and Conquer Generals Zero Hour Data\Maps"
          Write-Host "Move maps to $destination"
          New-Item -ItemType Directory -Path $destination -Force | Out-Null
          Move-Item -Path "$source\*" -Destination $destination -Force

      - name: Run Replay Compatibility Tests
        shell: pwsh
        run: |
          $exePath = "build/generalszh.exe"
          $arguments = "-jobs 4 -headless -replay *.rep"
          $timeoutSeconds = 10*60
          $stdoutPath = "stdout.log"
          $stderrPath = "stderr.log"

          if (-not (Test-Path $exePath)) {
              Write-Host "ERROR: Executable not found at $exePath"
              exit 1
          }

          # Note that the game is a gui application. That means we need to redirect console output to a file
          # in order to retrieve it.
          # Clean previous logs
          Remove-Item $stdoutPath, $stderrPath -ErrorAction SilentlyContinue

          # Start the process
          Write-Host "Run $exePath $arguments"
          $process = Start-Process -FilePath $exePath `
              -ArgumentList $arguments `
              -RedirectStandardOutput $stdoutPath `
              -RedirectStandardError $stderrPath `
              -PassThru

          # Wait with timeout
          $exited = $process.WaitForExit($timeoutSeconds * 1000)

          if (-not $exited) {
              Write-Host "ERROR: Process still running after $timeoutSeconds seconds. Killing process..."
              Stop-Process -Id $process.Id -Force
          }

          # Read output
          Write-Host "=== STDOUT ==="
          Get-Content $stdoutPath

          if ((Test-Path $stderrPath) -and (Get-Item $stderrPath).Length -gt 0) {
              Write-Host "`n=== STDERR ==="
              Get-Content $stderrPath
          }

          if (-not $exited) {
              exit 1
          }

          # Check exit code
          $exitCode = $process.ExitCode
          
          # The above doesn't work on all Windows versions. If not, try this: (see https://stackoverflow.com/a/16018287)
          #$process.HasExited | Out-Null # Needs to be called for the command below to work correctly
          #$exitCode = $process.GetType().GetField('exitCode', 'NonPublic, Instance').GetValue($process)
          #Write-Host "exit code $exitCode"

          if ($exitCode -ne 0) {
              Write-Host "ERROR: Process failed with exit code $exitCode"
              exit $exitCode
          }

          Write-Host "Success!"

      - name: Upload Debug Log
        if: always()
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: Replay-Debug-Log-${{ inputs.preset }}
          path: build/DebugLogFile*.txt
          retention-days: 30
          if-no-files-found: ignore

# Concurrent slices — ownership register

Several sessions work port slices in parallel. This file is how they avoid colliding: claim the paths
your slice owns *before* you start, and check that nobody else holds them. It is deliberately a plain
table rather than tooling — the cost of a stale row is a rebase, the cost of two slices silently
editing `dx8wrapper.cpp` is a day.

Hand-maintained. `STATUS.md`, by contrast, is generated; do not put measurements here.

## In flight

| Slice | Owns (paths) | Exit criterion | PR |
|---|---|---|---|
| _example_ `platform/audio-device` | `Core/Libraries/Source/OpenALAudioDevice/**` | every declared `AIL_*` symbol defined; `check-openal-symbols.py` green | #28 |
| `wire/lanmessage-64bit` | `Core/GameEngine/Include/GameNetwork/LANAPI.h`, `LANMessageWire.h`, `Core/GameEngine/Source/GameNetwork/LANAPI*.cpp`, `Core/GameEngine/Source/GameNetwork/GameInfo.cpp`, `{Generals,GeneralsMD}/Code/GameEngine/Source/GameClient/GUI/GUICallbacks/Menus/{LanGameOptionsMenu,NetworkDirectConnect,GameInfoWindow}.cpp`, `scripts/native-layout-test.py`, `scripts/ci/check-lanmessage-layout.py`, `docs/porting/lanmessage-64bit.md`, `docs/porting/ci-baselines/*.json` | `sizeof(LANMessage)` identical under `-m32` and `-m64` and `<= MAX_LANAPI_PACKET_SIZE`, asserted in a committed test; the 17 translation units that failed on that `static_assert` compile | tbd |
| `wire/widechar-disk-and-wire` | `Core/GameEngine/Include/Common/WideCharWire.h` (new), `Core/GameEngine/Source/GameClient/{GameText.cpp,LanguageFilter.cpp}` + `Core/GameEngine/Include/GameClient/LanguageFilter.h`, `Core/GameEngine/Source/Common/System/{Xfer.cpp,XferSave.cpp,XferLoad.cpp,XferCRC.cpp,LocalFile.cpp}`, `{Generals,GeneralsMD}/Code/GameEngine/Source/Common/System/DataChunk.cpp`, `{Generals,GeneralsMD}/Code/GameEngine/Source/Common/Recorder.cpp`, `Core/GameEngine/Include/GameNetwork/NetPacketStructs.h` + `Core/GameEngine/Source/GameNetwork/NetPacketStructs.cpp`, `scripts/ci/check-widechar-wire.py` (new), `docs/porting/widechar-wire.md` (new), the recommendation section of `docs/porting/widechar-fallout.md`, the `widechar-wire` step of `.github/workflows/native-port-ci.yml`, one line of `Core/GameEngine/CMakeLists.txt`; **borrowed, disclosed in the PR**: `Core/GameEngine/Include/GameNetwork/LANWireString.h` + `Core/GameEngine/Source/GameNetwork/LANWireString.cpp` (from `wire/lanmessage-64bit`, already on `main` — its two functions now call through the generic seam so there is one conversion, not two; `LANAPI.h` untouched and `check-lanmessage-layout.py` still green) | every external wide-text crossing on the single-player path reads and writes 16 bit code units with no `sizeof(WideChar)` left in it, proven by a `.csf` round trip whose negative control desynchronises exactly as the first native run did, and by both `wchar_t` widths producing byte-identical external data | this PR |
| `renderer/backend-coverage` | `spikes/renderer/src/{vulkan_backend.cpp,render_backend.h,state_translate.*,fixedfunc_tests.cpp}`, `spikes/renderer/shaders/**`, `spikes/renderer/tools/{backend-coverage-scan.py,backend-coverage-map.json,backend-coverage-baseline.json}`, `scripts/ci/check-backend-coverage.py`, `docs/porting/renderer-surface.md` | a committed coverage gate over the 62 (now 64) measured D3D8 entry points and every render/texture-stage state the engine sets; `check-backend-coverage.py` green with a measured before/after count, pixel-verified on lavapipe | this PR |
| `renderer/methods-and-d3dx` | `spikes/renderer/src/{vulkan_backend.cpp,render_backend.h,fixedfunc_tests.cpp}`, `spikes/renderer/shaders/**`, `spikes/renderer/tools/backend-coverage-baseline.json`, `Core/Libraries/Source/WWVegas/WWMath/{d3dx8math.cpp,CMakeLists.txt}`, `docs/porting/renderer-surface.md`, `docs/porting/native-build-report.md` and `docs/porting/ci-baselines/native-build-shimmed-level1-2.json` (regenerated, not hand-edited) | the coverage gate's measured backend-method count rises from 30/45 with a pixel assertion per newly covered method; the 5 D3DX symbols defined and gone from the native link's unresolved list | this PR |
| `renderer/d3dx8-math-entrypoints` | `Core/Libraries/Source/WWVegas/WWMath/d3dx8math.cpp`, `Core/Libraries/Source/WWVegas/WWMath/tests/d3dx8math_test.cpp` (new), `scripts/native-d3dx8math-test.py` (new), `docs/porting/d3dx8-math-seam.md` (new), the one D3DX include line of `Core/GameEngineDevice/Source/W3DDevice/GameClient/Water/W3DWater.cpp`, the D3DX-math step of the `native-build` job in `.github/workflows/native-port-ci.yml`; **borrowed, disclosed in the PR**: `scripts/native-port-shims/d3dx8math.h` (declarations only, from `compat/crt-and-widechar` — it is where the precedent puts them), and the serialised generated `docs/porting/{native-build-report.md,STATUS.md}` + `docs/porting/ci-baselines/native-build-shimmed-level1-2-3{,-4}.json` + the D3DX rows of `docs/porting/native-build.md` (measurement-only, regenerated by the scripts, never hand-merged) | the three D3DX compile failures (`W3DWater.cpp`, `pointgr.cpp`, `sortingrenderer.cpp`) are 0 and the five `D3DXMatrix*` symbols are defined and gone from the native link; the row-vector conventions (product order, translation row, singular-matrix contract) asserted numerically in CI and mutation-verified, with `d3dx8math.cpp` still empty on Windows | this PR |
| `platform/macos-window-compile` | `Core/Libraries/Source/WWVegas/WWLib/platform/platform_window_cocoa.mm`, `spikes/renderer/src/metal_surface_probe.mm`, `spikes/renderer/tools/macos-window-check.sh`, `spikes/renderer/CMakeLists.txt`, the Apple branch of `Core/Libraries/Source/WWVegas/WWLib/CMakeLists.txt`, `scripts/window-input-scan.py`, `docs/porting/window-event-loop.md`, the `window-seam-macos` job in `.github/workflows/native-port-ci.yml` | a green (not `continue-on-error`) `macos-15` job that compiles the Cocoa backend and asserts the Vulkan/Metal surface facts the runner can prove | this PR |
| `compat/crt-and-widechar` | `scripts/native-port-shims/**` (except `dinput.h`, `windows.h` window/input types), `Dependencies/Utility/**` compat headers, `docs/porting/crt-and-widechar-compat.md`, `scripts/ci/check-crt-compat.py`, plus the serialised `ci-baselines/*.json`, `scripts/native-port-probe.py` and `scripts/native_probe_targets.py` for the duration | native-build compile failures for the CRT/wide-char/misc-header cluster resolved; objects up, unresolved symbols down; baselines regenerated | #38 |
| `build/native-level3-and-thirdparty` | `scripts/native-build.py`, `scripts/native_probe_targets.py`, `scripts/ci/check-native-build-baseline.py`, `cmake/native/CMakeLists.txt`, `docs/porting/ci-baselines/native-build-shimmed-*.json`, `docs/porting/native-build.md`, `docs/porting/native-build-report.md`, the `native-build` job in `.github/workflows/native-port-ci.yml` | a level 3 (`GameEngineDevice` + entry point) build measured and ratcheted in CI, lzhl and zlib linked so the 9 third-party symbols resolve, and every undefined-symbol category attributable to a named file or a named cut | (this slice) |
| `renderer/staging-pool` | `spikes/renderer/src/{staging_workload.cpp,resource_lock_tests.cpp}`, the staging/lock path of `spikes/renderer/src/vulkan_backend.cpp` and the `ResourceStats` block of `render_backend.h`, `spikes/renderer/tools/surface-lock-audit.{py,json}`, `scripts/ci/check-staging-cost.py`, `docs/porting/ci-baselines/staging-cost-ceiling.json` (new file, no shared baseline edited), `docs/porting/renderer-resource-seam.md`, the `spike-resources` steps of `.github/workflows/native-port-ci.yml` | staging buffers pooled behind the unchanged D3D8-shaped lock/unlock contract, with a committed per-mode staging-cost ceiling that CI enforces and that the pre-pool behaviour provably violates; measured peak/steady-state residency in the resource-seam doc instead of "unknown" | (this slice) |
| `platform/win32-file-api` | `Core/Libraries/Source/WWVegas/WWLib/platform/platform_win32_compat.h`, `platform_win32_file.cpp`, `platform_win32_module.cpp`, `platform_win32_locale.cpp`, `platform_win32_stub.cpp`, the non-Windows branch of `Core/Libraries/Source/WWVegas/WWLib/CMakeLists.txt`, `GeneralsMD/Code/GameEngine/Source/Common/GameEngine.cpp`, `Core/GameEngine/Source/GameClient/GUI/IMEManager.cpp`, `scripts/native-port-shims/atlbase.h`, `scripts/ci/check-win32-undefined.py`, the Win32 gate step in the `native-build` job of `.github/workflows/native-port-ci.yml`, `docs/porting/win32-file-api-seam.md`, plus the serialised `docs/porting/ci-baselines/*.json` and `docs/porting/STATUS.md` for the duration | the native link's "Win32 API" undefined category drops from a measured 43 to 0, objects up, `check-win32-undefined.py` gates the category against a committed budget | (this slice) |
| `platform/debug-and-profile-libs` | `Core/Libraries/Source/debug/**` (except `debug_dlg/**`, `netserv/**`, `test*/**`), `Core/Libraries/Source/profile/**` (except `test1/**`), `scripts/native-stackwalk-test.py`, `scripts/ci/check-stackwalk-symbols.py`, `docs/porting/debug-and-profile-libs.md`, the crash/stack sections of `docs/porting/process-and-crash-seam.md`, the `debug-profile-seam` jobs in `.github/workflows/native-port-ci.yml`, plus `docs/porting/ci-baselines/*.json` for the duration | every translation unit `core_debug` and `core_profile_legacy` actually compile builds clean natively, links, and a committed test captures *and symbolises* a real backtrace through `DebugStackwalk` on x86-64 and arm64 | (this slice) |
| `platform/macos-hardware-verification` | `docs/porting/macos-hardware-verification.md`, `spikes/renderer/src/macos_window_metrics.mm`, `spikes/assets/src/path_probe.cpp`, plus the `zh-macos-window-metrics`/`zh-path-probe` targets in `spikes/{renderer,assets}/CMakeLists.txt`; **borrowed, portability-only, disclosed in the PR**: `Core/Libraries/Source/WWVegas/WWLib/platform/platform_window_cocoa.mm` and `docs/porting/window-event-loop.md` (from `platform/macos-window-compile`), `scripts/native-build.py` (from `build/native-level3-and-thirdparty`), `scripts/native-layout-test.py` (from `wire/lanmessage-64bit`), `scripts/ci/fetch-probe-deps.sh` | the window/Retina/input/fullscreen/Metal/asset facts CI cannot reach, measured on real Apple Silicon hardware and written down with the commands, separating measured from inferred and naming what a Mac still could not verify | this PR |
| `ci/replay-gate-manual` | `.github/workflows/check-replays.yml`, `scripts/ci/run-replays-local.ps1`, `scripts/ci/pack-gamedata.py`, `docs/porting/replay-check-gamedata.md`, the replay section of `TESTING.md` | the replay gate skips with a stated reason instead of failing red when no game data is configured, the manual run is a committed script rather than prose, and the game data can be packed from the machine that holds it rather than only from Windows | #54, this PR |
| `video/bink-excision-and-harness-headers` | `Core/GameEngineDevice/Include/VideoDevice/**`, `Core/GameEngineDevice/Source/VideoDevice/**`, `Core/GameEngineDevice/CMakeLists.txt`, `GeneralsMD/Code/GameEngineDevice/Include/W3DDevice/GameClient/W3DGameClient.h`, `scripts/ci/fetch-probe-deps.sh`, `scripts/ci/check-video-headers.py`, `docs/porting/video-and-harness-headers.md`, plus the serialised `scripts/native-port-probe.py`, `docs/porting/ci-baselines/*.json` and `docs/porting/STATUS.md` for the duration; **borrowed, disclosed in the PR**: the `binkstub` link line of `GeneralsMD/Code/Main/CMakeLists.txt` (one build guard, from `platform/window-seam-wiring`), and the diagnostic-attribution block of `scripts/native-build.py` plus the regenerated `docs/porting/native-build-report.md` (measurement-only, from `build/native-level3-and-thirdparty`) | the `bink.h` and missing-header (`libavcodec/avcodec.h`, `stb_image_write.h`, `Common/File.h`) compile-failure classes are 0, provisioned reproducibly in CI, gated by `check-video-headers.py`; objects up and unresolved symbols down | (this slice) |
| `renderer/native-compile-and-entrypoint` | `Core/Libraries/Source/WWVegas/WW3D2/{renderbackend.h,d3d8renderbackend.h,dx8wrapper.h,dx8wrapper.cpp}`, `Core/GameEngineDevice/Include/MilesAudioDevice/MilesAudioManager.h`, `Core/GameEngineDevice/Source/MilesAudioDevice/MilesAudioManager.cpp`, `scripts/native-port-probe.py`, `docs/porting/renderer-seam.md`, `docs/porting/native-build.md`, `docs/porting/native-build-report.md`, plus the serialised `docs/porting/ci-baselines/*.json` and generated `docs/porting/STATUS.md` for the duration; **borrowed, portability-only, disclosed in the PR**: the `HANDLE`-typed member of `MilesAudioManager.h` becomes the `void*` that `HANDLE` is, so the header stops needing the Win32 type surface `platform/win32-file-api` owns — no shim or `platform_win32_*` file is touched, and `docs/porting/ci-baselines/win32-undefined-budget.json` (from `platform/win32-file-api`) is widened from 0 to the three cursor calls `W3DMouse.cpp` now links against | the `TheD3D8RenderBackend` compile-failure class is 0; `check-d3d8-surface.py` still 0 and `check-backend-coverage.py` still matching; `GeneralsMD/Code/Main` reduced to a single remaining diagnostic (`bink.h`, the Bink/FFmpeg slice's), so it produces objects only once that slice lands | this PR |
| `platform/window-seam-wiring` | `Core/GameEngine/Include/GameClient/PlatformWindowHost.h`, `Core/GameEngine/Source/GameClient/PlatformWindowHost.cpp`, `Core/GameEngine/Source/Common/System/Debug.cpp`, `Core/GameEngine/Source/GameClient/Input/Keyboard.cpp`, `Core/GameEngineDevice/**/Win32Device/GameClient/**`, `GeneralsMD/Code/GameEngineDevice/Source/Win32Device/Common/Win32GameEngine.cpp`, `GeneralsMD/Code/Main/**`, `Core/Libraries/Source/WWVegas/WWLib/platform/platform_dialog.*`, `scripts/ci/check-window-seam-wiring.py`, `docs/porting/window-event-loop.md` | the engine's non-Windows window/message/input path goes through `platform_window.h`; `check-window-seam-wiring.py` green; native-build probe-clean rises | (this slice) |
| `renderer/lock-hazard-and-preserve` | the dirty-bit and staging/lock path of `spikes/renderer/src/vulkan_backend.cpp`, the `ResourceStats` block of `spikes/renderer/src/render_backend.h`, `spikes/renderer/src/{resource_lock_tests.cpp,staging_workload.cpp}`, `scripts/ci/check-staging-cost.py`, `docs/porting/ci-baselines/staging-cost-ceiling.json`, `docs/porting/renderer-resource-seam.md` | decisions 1 and 2 of `decisions-resolved.md` implemented: a GPU-dirty bit at every enumerated write funnel with a test that fails if a *clean* read transfers anything, and pooled staging that preserves contents by default; `zh-resource-lock-tests`, `check-staging-cost.py` and the pixel gate green on lavapipe with the validation layer silent, and the staging cost re-measured rather than re-quoted | (this slice) |
| `platform/gdi-font` | `Core/Libraries/Source/WWVegas/WWLib/platform/platform_win32_gdi_font.cpp`, `Core/Libraries/Source/WWVegas/WWLib/platform/tests/gdi_font_metrics_dump.cpp`, `Core/Libraries/Source/WWVegas/WWLib/win.h`, the non-Windows branch of `Core/Libraries/Source/WWVegas/WWLib/CMakeLists.txt`, the GDI block of `scripts/native-port-shims/windows.h`, `scripts/ci/{check-font-metrics.py,record-gdi-font-metrics.sh,font-metrics-reference.json}`, the stb line of `scripts/ci/fetch-probe-deps.sh`, the font-metrics step of the `native-build` job in `.github/workflows/native-port-ci.yml`, `docs/porting/gdi-font-seam.md`, plus the serialised `scripts/native-build.py`, `scripts/native-port-probe.py`, `docs/porting/ci-baselines/*.json` (including the one `window-input-scan.json` line for the seam's `GetDC(HWND)`) and `docs/porting/STATUS.md` for the duration | the `HFONT` compile-failure class drops from a measured 18 to 0, objects up against `native-build-shimmed-level1-2-3.json`, and `check-font-metrics.py` gates the portable rasteriser's advance widths and line height against recorded GDI reference metrics | (this slice) |
| `online/embedded-browser-excision` | `Core/GameEngine/Include/GameNetwork/WOLBrowser/{WebBrowser.h,FEBDispatch.h}`, `Core/GameEngine/Source/GameNetwork/WOLBrowser/WebBrowser.cpp`, `GeneralsMD/Code/GameEngineDevice/Include/W3DDevice/GameClient/W3DWebBrowser.h`, `GeneralsMD/Code/GameEngineDevice/Source/W3DDevice/GameClient/W3DWebBrowser.cpp`, `GeneralsMD/Code/GameEngineDevice/Include/Win32Device/Common/Win32GameEngine.h`, `Core/Libraries/Source/EABrowserDispatch/CMakeLists.txt`, `scripts/ci/check-embedded-browser.py`, `docs/porting/embedded-browser-seam.md`, the embedded-browser gate step of the `native-build` job in `.github/workflows/native-port-ci.yml`; **borrowed, disclosed in the PR**: the serialised `docs/porting/ci-baselines/*.json` + generated `docs/porting/{STATUS.md,native-build-report.md}` for the duration, `scripts/native-build.py` and `docs/porting/native-build.md` (from `build/native-level3-and-thirdparty`: the generated `BuildVersion.h`/`GeneratedVersion.h` the entry point needs, using the game's own `main()` instead of a stub, and an FFmpeg undefined-symbol category), and three `#ifdef RTS_HAS_EMBEDDED_BROWSER` lines around the ATL module in `GeneralsMD/Code/GameEngine/Source/Common/GameEngine.cpp` (from `platform/win32-file-api`; nothing else in that file touched) | the `CComObject`/`IDispatch` compile-failure class is 0 and **`GeneralsMD/Code/Main` produces an object and an archive for the first time**, gated by `check-embedded-browser.py`; every unresolved symbol of the resulting game-target link attributed to a named category | (this slice) |
| `compat/win32-runtime-and-crt-gaps` | `Core/Libraries/Source/WWVegas/WWLib/platform/platform_win32_kernel.cpp`, `platform_win32_user.cpp`, `platform/tests/win32_runtime_test.cpp`, the two new lines in the non-Windows branch of `Core/Libraries/Source/WWVegas/WWLib/CMakeLists.txt`, `Dependencies/Utility/Utility/{string_compat.h,d3d8_compat.h}`, the sync/heap/`lstr`/`MB_*`/`VK_*`/power blocks of `scripts/native-port-shims/windows.h`, `Core/GameEngineDevice/Source/StdDevice/Common/StdLocalFileSystem.cpp`, `Core/GameEngineDevice/Source/W3DDevice/GameClient/W3DShaderManager.cpp`, `scripts/native-win32-runtime-test.py`, one new step in `.github/workflows/native-port-ci.yml`, `docs/porting/win32-runtime-and-crt-gaps.md`, plus the serialised `docs/porting/ci-baselines/*.json` and generated `docs/porting/STATUS.md` for the duration; **borrowed, portability-only, disclosed in the PR**: one `#include` line in `Core/GameEngineDevice/Source/MilesAudioDevice/MilesAudioManager.cpp` (from `renderer/native-compile-and-entrypoint`, whose `void*` member this finishes) | the 8 compile failures in this cluster (`HANDLE`, `InterlockedExchangePointer`, `DriverVersion`, `VK_F5`, `lstrcpyn`, `MB_APPLMODAL`, `unNormalized`, `_strdup`) are 0, no new undefined Win32 symbol (`check-win32-undefined.py` budget unchanged), and `native-win32-runtime-test.py` asserts the semantics of every entry point this adds | this PR |
| `online/absent-menu-seam` | `Dependencies/Utility/Utility/hresult_compat.h` (new), `Core/Libraries/Source/WWVegas/WWDownload/{downloaddefs.h,ftpdefs.h}`, `Core/GameEngine/Source/GameNetwork/GameSpy/Thread/{GameResultsThread.cpp,PeerThread.cpp}`, `scripts/ci/check-online-absent-seam.py`, `docs/porting/ci-baselines/online-absent-seam.json` (new file), `docs/porting/online-absent-menu-seam.md`; **borrowed, disclosed in the PR**: `Dependencies/Utility/Utility/socket_compat.h` and `Dependencies/Utility/CMakeLists.txt` (four `WSAE*` aliases and one source-list line, from `compat/crt-and-widechar`), and the regenerated `docs/porting/native-build-report.md` + `docs/porting/ci-baselines/*.json` + `docs/porting/STATUS.md` (measurement-only, serialised) | the single-player menu path (`MainMenu.cpp`, `DownloadMenu.cpp`, `DownloadManager.cpp` and the two portable GameSpy threads) compiles natively with the online path absent, gated by `check-online-absent-seam.py` naming every unresolved GameSpy SDK symbol; the three units that need Win32 surfaces other slices own stay failing under a named reason | (this slice) |
| `online/embedded-browser-dx8-layer` | `Core/Libraries/Source/WWVegas/WW3D2/{dx8webbrowser.h,dx8webbrowser.cpp}`, `Core/Libraries/Source/EABrowserEngine/CMakeLists.txt` (one `RTS_HAS_EMBEDDED_BROWSER` definition, needed for the Windows link), `scripts/ci/check-embedded-browser.py`, `docs/porting/embedded-browser-seam.md`; **borrowed, portability-only, disclosed in the PR**: one `IsIconic(HWND)` declaration in the user32 block of `scripts/native-port-shims/windows.h` and the one `IsIconic` name added to `docs/porting/ci-baselines/win32-undefined-budget.json` (from `platform/win32-file-api`), one include-path line for `GeneralsMD/Code/Main` in the `GeneralsMD/Code/GameEngineDevice` target of `scripts/native-port-probe.py` (measurement-only, matching that library's own `zi_main` link), and the serialised `docs/porting/ci-baselines/native-build-shimmed-level*.json` + generated `docs/porting/native-build-report.md` for the duration (regenerated, never hand-merged). **Not touched:** `dx8wrapper.{h,cpp}` — its browser blocker is fixed from the header, and its remaining `DriverVersion` failure is `compat/win32-runtime-and-crt-gaps`' | the `LPDISPATCH` compile-failure class is 0: `dx8webbrowser.cpp` and `W3DDisplay.cpp` compile with the control absent, gated by `check-embedded-browser.py` over the DX8 layer and its derived `ENABLE_EMBEDDED_BROWSER` switch | (this slice) |
| `build/native-level4-renderer-audio` | `scripts/native-build.py` (the `LEVELS` table and its comment only), `docs/porting/ci-baselines/native-build-shimmed-level1-2-3-4.json` (new file), the new `native-build-renderer` job in `.github/workflows/native-port-ci.yml`, `docs/porting/native-build.md`, plus the serialised generated `docs/porting/native-build-report.md` and `docs/porting/ci-baselines/native-build-shimmed-level1-2-3.json` for the duration (regenerated, never hand-merged); **borrowed, portability-only, disclosed in the PR**: the kernel32 string spellings (`lstrcpy`/`lstrcpyn`/`lstrcat`/`lstrcmp`/`lstrcmpi`/`lstrlen`) and `_strdup` in `Dependencies/Utility/Utility/string_compat.h`, which is the one dominant cheap cause among the new failures and is a CRT-alias header rather than any slice's implementation file | the renderer/audio layer is *built*, so the "defined in a layer not built here (renderer / audio)" undefined category drops from a measured 272 to 0 and every remaining compile failure is classified into a named cause with an owning slice | (this slice) |
| `build/strict-link-and-pile-split` | `scripts/native-build.py` (the strict-link path, the provider/pile attribution and the report sections they add), `scripts/ci/check-native-build-baseline.py`, `docs/porting/startability.md` (new file), the level 1-4 build step of `.github/workflows/native-port-ci.yml`, `docs/porting/native-build.md`; **borrowed, measurement-only, disclosed in the PR**: the serialised `docs/porting/ci-baselines/native-build-shimmed-level1-2-3{,-4}.json` and the generated `docs/porting/{native-build-report.md,STATUS.md}` for the duration (regenerated by the scripts, never hand-merged) | a strict link that fails honestly — no `--warn-unresolved-symbols`, non-zero exit, no file — with all 250 unresolved symbols matched against the `nm` scan and each assigned to one of five piles, of which only `no-definition-anywhere` (a measured 9) is port work; both that count and the strict total ratcheted in CI, and `startability.md` defining "startable" in conditions a job can check | (this slice) |
| `compat/ww3d2-and-download-headers` | `scripts/native-port-shims/{windowsx.h,ddraw.h}` (new), the AVI surface of `scripts/native-port-shims/vfw.h`, `mmioFOURCC` in `scripts/native-port-shims/mmsystem.h`, the `OF_*`/`GlobalHandle`/`SetRect` lines and the `_mkdir` comment in `scripts/native-port-shims/{windows.h,direct.h}`, `Core/Libraries/Source/WWVegas/WW3D2/{dx8caps.cpp,w3d_dep.cpp}`, `Core/Libraries/Source/WWVegas/WWDownload/{Download.cpp,FTP.cpp,ftp.h,registry.cpp}`, `Core/Libraries/Source/WWVegas/WWLib/platform/platform_win32_vfw.cpp` (new) plus its one line in that library's non-Windows `CMakeLists.txt` branch, `Dependencies/Utility/Utility/path_compat.h` (new) plus its one line in `Dependencies/Utility/CMakeLists.txt`, `scripts/ci/check-download-seam.py` (new) and its one new step in the level 1-4 job of `.github/workflows/native-port-ci.yml`, `docs/porting/ww3d2-and-download-headers.md`, plus the serialised `docs/porting/ci-baselines/*.json`, generated `docs/porting/{STATUS.md,native-build-report.md}` and the renderer/audio/download target rows of `scripts/native-port-probe.py` + the `cmake_dirs` line of `scripts/native_probe_targets.py` for the duration; **borrowed, portability-only, disclosed in the PR**: `GlobalHandle()` in `Core/Libraries/Source/WWVegas/WWLib/platform/platform_win32_module.cpp` and `SetRect()` in `platform_win32_user.cpp` (from `compat/win32-runtime-and-crt-gaps`; appended, nothing else in either file touched) and two `WSAE*` aliases in `Dependencies/Utility/Utility/socket_compat.h` (from `compat/crt-and-widechar`) | the ten WW3D2/WWDownload compile failures (`windowsx.h`, `ddraw.h`, `DriverVersion`, `textdraw.cpp`, `size_t`, `Common/Debug.h`, `LPCSTR`, `KEY_READ`, `sku`) are 0 -- 952/971 objects to 962/971 -- `WWDownload` produces an archive in the level 1-4 build for the first time, the measured denominator no longer contains a source CMake comments out, and `check-download-seam.py` gates all of it | (this slice) |
| `build/native-audio-backend-link` | `scripts/native-build.py` (the audio-backend support archive, the OpenAL link line and the `al*`/`alc*` category only), the OpenAL block of `scripts/ci/fetch-probe-deps.sh`, `scripts/ci/check-audio-backend-linked.py` (new file), the two `Gate the audio backend in the link` steps and the `libopenal-dev` line of `.github/workflows/native-port-ci.yml`, the audio section of `docs/porting/native-build.md` and the harness paragraph of `docs/porting/audio-device-seam.md`, plus the serialised generated `docs/porting/native-build-report.md`, `docs/porting/ci-baselines/native-build-shimmed-level1-2-3{,-4}.json` and `docs/porting/STATUS.md` for the duration (regenerated, never hand-merged) | the harness builds and links `Core/Libraries/Source/OpenALAudioDevice`, so the "Miles Sound System" undefined category drops from a measured 89 (levels 1-4) and 60 (levels 1-3) to 0 with no `AIL_*` stubbed, gated by `check-audio-backend-linked.py` | (this slice) |
| `renderer/dx8wrapper-native-compile` | `Core/Libraries/Source/WWVegas/WW3D2/dx8wrapper.cpp` (the serialised renderer chokepoint, held for this slice), the user32 window-metrics / `MONITORINFO` / GDI-gamma block of `scripts/native-port-shims/windows.h` (the window-and-input types `compat/crt-and-widechar` exempts from its claim), `docs/porting/dx8wrapper-native-compile.md` (new); **borrowed, measurement-only, disclosed in the PR**: `docs/porting/ci-baselines/native-build-shimmed-level1-2-3-4.json` and `docs/porting/native-build-report.md` (regenerated by `scripts/native-build.py`, never hand-merged), the eight Win32 names added to `docs/porting/ci-baselines/win32-undefined-budget.json` by `check-win32-undefined.py --update`, and a five-line superseded note at the top of `docs/porting/startability.md` (no figure in its body rewritten) | `dx8wrapper.cpp` compiles at 64-bit off Windows with no `_WIN32` and no stub: level 1-4 compile failures 4 → 3 (the three remaining all cut-scope GameSpy), objects 968 → 969, the `compile-blocked` pile 108 → 18 and the strict-link total 250 → 173, with the 13 newly *visible* undefined symbols attributed to the window/input and D3DX slices rather than implemented here | this PR |
| `online/gamespy-path-excision` | the GameSpy-library split of `Core/GameEngine/CMakeLists.txt` (the `RTS_HAS_GAMESPY` / `gamespy::gamespy` block only), `Core/GameEngine/Include/GameNetwork/GameSpy/MainMenuUtils.h`, `Core/GameEngine/Source/GameNetwork/GameSpy/{MainMenuUtils.cpp,StagingRoomGameInfo.cpp}`, `Core/GameEngine/Source/GameNetwork/GameSpy/Thread/{PingThread.cpp,PeerThread.cpp,BuddyThread.cpp,PersistentStorageThread.cpp}`, the `ghttp` lines of `{Generals,GeneralsMD}/Code/GameEngine/Source/GameClient/GUI/GUICallbacks/Menus/MainMenu.cpp` and the `getQR2HostingStatus()` lines of `{Generals,GeneralsMD}/Code/GameEngine/Source/GameClient/GUI/GUICallbacks/{InGameChat.cpp,Menus/WOLLobbyMenu.cpp,Menus/WOLGameSetupMenu.cpp}`, `docs/porting/online-path-excision.md`, `docs/porting/ci-baselines/online-absent-seam.json`; **borrowed, disclosed in the PR**: the four `_open`/`_close`/`_O_CREAT`/`_O_RDWR` lines of `Dependencies/Utility/Utility/path_compat.h` (from `compat/ww3d2-and-download-headers`, appended to its non-Windows branch), and the serialised `docs/porting/ci-baselines/native-build-shimmed-level1-2-3-4.json` + generated `docs/porting/STATUS.md` for the duration (regenerated by the scripts, never hand-merged). **Not touched:** `dx8wrapper.cpp` (slice 1), the 9 `no-definition-anywhere` symbols (slice 3), the link configuration and harness (slice 4) | the three GameSpy compile failures (`HANDLE`, `AsnObjectIdentifier`, `HOSTENT`) are 0 and `Core/GameEngine` builds 210/210, with `cut-scope-not-linked` 82 -> **0** and `no-definition-anywhere` still 9, achieved by not linking the SDK off Windows rather than by linking or stubbing it — no GameSpy function defined anywhere in the diff — and the per-symbol budget of `check-online-absent-seam.py` tightened 81 -> 0 with its five menu-path units still required by name | (this slice) |
| `platform/window-gdi-and-d3dx-entrypoints` | `Core/Libraries/Source/WWVegas/WWLib/platform/platform_win32_user.cpp` (appended, held for this slice), `platform/platform_window.h`, `platform/platform_window_sdl2.cpp`, `platform/platform_window_cocoa.mm`, `platform/tests/win32_user32_test.cpp` (renamed from `win32_cursor_test.cpp`), `Core/Libraries/Source/WWVegas/WW3D2/{d3dx8fvf.cpp,d3dx8texture.cpp,d3dx8texture.h,d3dx8shader.cpp,tests/d3dx8*_test.cpp}` (new) plus the four lines they add to that library's `CMakeLists.txt`, `Core/Libraries/Source/WWVegas/WWAudio/listenerhandle.h` (one dead declaration deleted), `scripts/native-win32-user32-test.py` (renamed from `native-win32-cursor-test.py`), `scripts/native-d3dx8-entrypoints-test.py` (new), the two new test steps of the `native-build` job in `.github/workflows/native-port-ci.yml`, `docs/porting/{window-gdi-seam.md,d3dx8-texture-seam.md}` (new), the user32/`MONITORINFO`/`WS_*` block of `scripts/native-port-shims/windows.h` (declarations only, released by `renderer/dx8wrapper-native-compile` when #79 merged); **borrowed, measurement-only, disclosed in the PR**: `docs/porting/ci-baselines/native-build-shimmed-level1-2-3{,-4}.json`, `docs/porting/ci-baselines/win32-undefined-budget.json` (narrowed to empty), `docs/porting/ci-baselines/window-input-scan.json` (re-scanned, +66 references, 51 of them the new test file) and the generated `docs/porting/{native-build-report.md,STATUS.md}` (regenerated by the scripts, never hand-merged); **borrowed, one class and one assignment added, disclosed in the PR**: `spikes/renderer/tools/d3d8-lock-classes.json` (the C9 class for the two surface locks `D3DXLoadSurfaceFromSurface` introduces — `renderer/staging-pool` owns `renderer-resource-seam.md` and `surface-lock-audit.*`, which this slice does not touch) | the `no-definition-anywhere` pile 22 → 5 at levels 1-4 and 6 → 2 at levels 1-3, with a behaviour test per implemented symbol and an argued, loudly-failing non-implementation for the rest; `Win32 API` 12 → 0, so `win32-undefined-budget.json`'s allowance is empty and `check-win32-undefined.py` gates at zero; the 12 seam functions the implementations newly reference reported, not hidden | this PR |
| `build/strict-link-produces-a-binary` | `scripts/native-build.py` (the window-backend choice, the gitinfo generation, the FFmpeg/SDL2 link line, the include order, the evidence-first attribution and the executable description), `scripts/ci/check-native-build-baseline.py` (the inverted ratchet), `scripts/native-build-categorise-test.py` (new), the FFmpeg-library block of `scripts/ci/fetch-probe-deps.sh`, `Core/Libraries/Source/WWVegas/WW3D2/{d3dx8texcreate.cpp,d3dx8texcreate.h,tests/d3dx8texcreate_test.cpp}` (new) plus the five lines they add to that library's `CMakeLists.txt`, the `.mm` branch of `cmake/native/CMakeLists.txt`, `scripts/native-d3dx8-entrypoints-test.py` (one suite), `scripts/native-port-probe.py` (the `IG_DEBUG_STACKTRACE` define only), the `native-build`/`native-build-renderer`/`lint`/`native-probe`/`lanmessage-layout` jobs of `.github/workflows/native-port-ci.yml`, `docs/porting/startability.md` (rewritten), `docs/porting/{decisions-resolved.md,native-build.md,d3dx8-texture-seam.md}`; **borrowed, measurement-only, disclosed in the PR**: `docs/porting/ci-baselines/native-build-shimmed-level1-2-3{,-4}.json` and the generated `docs/porting/{native-build-report.md,STATUS.md}` (regenerated by the scripts, never hand-merged) | `--strict-link` produces an executable and `strict_link.binary_produced` is true: 0 unresolved symbols, nothing stubbed to get there, the file described from its own header, and its first startup failure recorded. CI's assertion inverts from "the unresolved count must not grow" to "the link must not break", with the executable's word size and machine gated too | this PR |
| `audio/end-to-end-probe` | `Core/Libraries/Source/OpenALAudioDevice/tests/openal_audio_probe.cpp` (new), `scripts/native-audio-probe.py` (new), `docs/porting/audio-path-probe.md` (new). **Nothing else** — this is an exploration slice and fixes none of the defects it records; `Core/Libraries/Source/OpenALAudioDevice/*.cpp`, `MilesAudioManager.cpp` and every baseline are untouched | the OpenAL-backed `AIL_*` layer driven with the engine's own call sequences on a 64-bit host with the mix captured and measured, not inferred: 17 committed gates over what works today (device open, listener, 2D/3D handle grants, PCM and IMA ADPCM images, ADPCM decode agreeing with ffmpeg, hard pan) plus a printed, evidenced observation per path that does not work | this PR |
| `diagnostics/init-failure-reporting` | `Core/GameEngine/Include/Common/InitFailure.h` (new), `Core/GameEngine/Source/Common/System/tests/init_failure_test.cpp` (new), `scripts/{init-reporting-scan.py,native-init-failure-test.py}` (new), `docs/porting/init-failure-reporting.md` (new), `docs/porting/ci-baselines/init-reporting.json` (new file), the two new steps in the `source-scans` and `native-build` jobs of `.github/workflows/native-port-ci.yml`; **narrow, disclosed in the PR**: the five failure-reporting sites of `Core/GameEngine/Source/GameClient/GameText.cpp` (the failure-reporting seam only — `parseCSF`, `parseStringFile` and every `wchar_t` expression are untouched and belong to slice A) and the `catch (ErrorCode)` clause of `GeneralsMD/Code/GameEngine/Source/Common/GameEngine.cpp`. **Not touched:** the subsystem list, `SubsystemInterface`, `INIException` | all 46 init entry points under `GameEngine::init()` enumerated with their status availability, whether anyone checks it and the consequence of failure; the string manager's silent returns and the swallowed non-D3D `ErrorCode` are process-level and name the subsystem, the operation and the file; success paths byte-identical; ratcheted per entry by `init-reporting-scan.py --check` and proven by a negative control that shows the pre-fix shape was undiagnosable | this PR |
| `build/macos-residue-and-measurement-defects` | `scripts/native-build.py` (the symbol namespace, the system/framework discount, the entry-point anchor and the architecture/Rosetta description), `scripts/ci/check-native-build-baseline.py` (the entry-point and architecture gate), `scripts/native-build-categorise-test.py`, `Core/Libraries/Source/WWVegas/WWLib/regexpr.cpp`, `docs/porting/startability.md` (rewritten as a status table), `docs/porting/native-build.md` (the macOS build section), `.agents/skills/native-port-measure/SKILL.md` (the macOS rung); **borrowed, disclosed in the PR**: `scripts/native-port-shims/gnu_regex.h` — **deleted**, from `compat/crt-and-widechar`: it existed only for `regexpr.cpp`'s GNU `re_*` calls, which this slice replaces with POSIX `<regex.h>`, so keeping it would leave a shim documenting a dependency the tree no longer has; the two `regexpr`/GNU-regex rows of `docs/porting/crt-and-widechar-compat.md` (same reason), and the serialised `docs/porting/ci-baselines/*.json` + generated `docs/porting/{native-build-report.md,STATUS.md}` for the duration (regenerated by the scripts, never hand-merged) | the one macOS compile failure `#87` left (`WWLib/regexpr.cpp`, GNU-only `reg_syntax_t`) is resolved by a decided-and-recorded POSIX port rather than an exclusion; `strict_link.agrees_with_nm` can hold on Mach-O because the discount list and the entry-point scan read Mach-O's own spellings, measured from the toolchain instead of switched on `sys.platform`; and a missing entry point in a build that contains `GeneralsMD/Code/Main` stops the run instead of being replaced by a generated `main()`, asserted in the harness test and in the baseline gate | this PR |
| `renderer/backend-integration` | `Core/Libraries/Source/WWVegas/WW3D2/{vulkanrenderbackend.h,vulkanrenderbackend.cpp,tests/native_render_run.cpp}` (new) plus the four lines they add to that library's `CMakeLists.txt`, the one non-Windows initialiser line of `dx8wrapper.cpp` (the serialised renderer chokepoint, held for this slice), `scripts/native-render-backend-run.py` (new), the render-backend block of `scripts/native-build.py`, the WW3D2 include line of `scripts/native-port-probe.py`, `docs/porting/renderer-integration.md` (new), the second-implementation note at the top of `docs/porting/renderer-seam.md`, one superseded note in `docs/porting/startability.md` and one in `docs/porting/first-native-run-arm64.md` (no figure in either body rewritten); **borrowed, disclosed in the PR**: `spikes/renderer/{CMakeLists.txt,src/render_backend.h,src/vulkan_backend.cpp}` (from `renderer/backend-coverage` and `renderer/lock-hazard-and-preserve`, both merged — the backend sources become the `zh-render-backend` static library the engine links, and the API gains `Present()`, lockable vertex/index buffers and adapter enumeration; no lock funnel, no shader and no pixel path touched, so `d3d8-lock-classes.json` and the coverage baseline are unchanged), and the serialised `docs/porting/ci-baselines/native-build-shimmed-level1-2-3{,-4}.json`, `docs/porting/ci-baselines/window-input-scan.json` (re-scanned by `--check --update`: the adapter's cursor, gamma and present-window *refusals* mention those Win32 names, +14 in-scope references and its two new files, none of them a new Win32 dependency) + generated `docs/porting/{native-build-report.md,STATUS.md}` (regenerated by the scripts, never hand-merged), plus the Vulkan clone in `scripts/ci/fetch-probe-deps.sh` and the `libvulkan-dev`/`glslang-tools` line of the two native-build jobs in `.github/workflows/native-port-ci.yml` | `DX8Wrapper`'s `RenderBackend` is a real object off Windows: `DX8Wrapper::Init()`, the device enumeration and `CreateDevice` all succeed against a live Vulkan device, objects 977 → 978 with the strict link still clean and an executable produced, the spike ladder still 0/480000 pixels and 0 validation messages, and the next wall measured under gdb with its first failing call named rather than worked around | this PR |
| `renderer/d3dx-route-and-first-frame` | `Core/Libraries/Source/WWVegas/WW3D2/{renderbackend.h,d3d8renderbackend.h,d3d8renderbackend.cpp,d3dx8texcreate.h,d3dx8texcreate.cpp,vulkanrenderbackend.h,vulkanrenderbackend.cpp,tests/d3dx8texcreate_test.cpp,tests/native_render_run.cpp}`, the one non-Windows `Peek_Render_Backend()` definition in `dx8wrapper.cpp` (the serialised renderer chokepoint, held for this slice), `scripts/native-render-backend-run.py`, the render-backend block of `scripts/native-build.py`, `scripts/ci/check-swapchain-compiled.py` (new), the one new step of the `native-build-renderer` job in `.github/workflows/native-port-ci.yml`, `docs/porting/renderer-first-frame.md` (new) and two superseded notes plus four ledger rows in `docs/porting/renderer-integration.md` (no figure in its body rewritten); **borrowed, disclosed in the PR**: `spikes/renderer/src/{vulkan_backend.cpp,main.cpp}` (the swapchain-less `Present()` refusal and the headless spike's `End_Scene(false)` — no lock funnel, shader or pixel path touched, so `d3d8-lock-classes.json`, the coverage baseline and the spike's reference image are unchanged) and `spikes/renderer/tools/d3d8-direct-allowlist.json` (3 → 4 sites, all in `d3dx8texcreate.cpp`, re-budgeted by `check-d3d8-surface.py --update`) | the engine's own path creates its textures through the backend and **presents a frame** whose contents are read back and measured (800x600, 480000/480000 pixels within 2/255 of the clear colour, `FrameCount` advanced 3, 0 validation messages), with the mip-level surface wall behind the D3DX one implemented through the existing lock funnel and `CopyRects`/`SetRenderTarget` on such a surface refused in the ledger; a swapchain-less backend can no longer report a successful `Present()`, gated at build time and refused at run time | this PR |
| `platform/path-separator-seam` | `Core/GameEngine/Source/Common/System/FileSystem.cpp` (the non-Windows enumeration respelling only), `Core/GameEngine/Include/GameClient/MapUtil.h` + `Core/GameEngine/Source/GameClient/MapUtil.cpp` (the cache-key canonicalization and the one `fopen` in `writeCacheINI`), `Core/GameEngine/Source/Common/INI/INIMapCache.cpp` (one line), `Core/Libraries/Source/WWVegas/WWLib/platform/platform_path.{h,cpp}` (`Open_Stream()` appended to both branches), `spikes/sim/src/sim_probe.cpp`, `scripts/ci/check-path-separator-keys.py` (new), the one new step of the `native-build-debug` job in `.github/workflows/native-port-ci.yml`, `docs/porting/path-separator-seam.md` (new). **Not touched:** `Core/GameEngineDevice/Source/StdDevice/Common/StdLocalFileSystem.cpp` (`compat/win32-runtime-and-crt-gaps`), the `Win32GameEngine` factory, the save/replay path `fopen` sites, the texture-listing call site (concurrent video slice), and every `docs/porting/ci-baselines/*.json` — no translation unit added or removed, so no baseline is regenerated | both spellings of a map resolve to one map cache entry with `m_isMultiplayer` intact, proven by a committed negative control that fails on the pre-seam code (`missed=2` and a segfault) and passes after; the `MapUtil.cpp:536` assertion and the release-configuration "0 maps found" it hides both closed on retail data, with identical map CRCs before and after | this PR |

Remove your row when the PR merges.

## Serialised paths

At most **one** in-flight slice may touch each of these, because a second concurrent edit produces a
conflict that is not mechanical to resolve, or a measurement that cannot be attributed:

- `docs/porting/ci-baselines/*.json` — two slices refreshing baselines produce numbers neither can
  reproduce.
- `scripts/native-port-probe.py` and `scripts/porting-status.py` — changing the measurement while
  someone else is measuring invalidates both results.
- `Core/Libraries/Source/WWVegas/WW3D2/dx8wrapper.{h,cpp}` and `d3d8renderbackend.cpp` — the renderer
  chokepoint; every renderer slice wants it.
- the root `CMakeLists.txt` and `cmake/*.cmake` — build wiring conflicts are silent until link time.

If your slice needs a serialised path that someone else holds, resequence rather than sharing it.

## Rules that keep concurrent work compatible

1. Branch from current `main`, never from another slice's branch.
2. One seam per PR; no drive-by edits outside your claimed paths.
3. Merge one port PR at a time; after each merge, every in-flight slice rebases onto `main` and
   re-measures before quoting a figure.
4. Additive over invasive: add a seam, a shim or a new implementation file rather than reshaping a
   header that other slices are reading.
5. Prefer extending `scripts/native-port-shims/` and `scripts/ci/` over editing shared engine headers.
6. If two slices genuinely need the same file, that is an escalation, not a merge conflict to be
   resolved later.
| `renderer/copyrects-representation` | the `Copy_Rects` path and `SurfaceHandle` format fields of `spikes/renderer/src/vulkan_backend.cpp`, the `Copy_Rects` contract comment in `spikes/renderer/src/render_backend.h`, the CopyRects cases of `spikes/renderer/src/resource_lock_tests.cpp`, `docs/porting/main-menu-2d-composition.md` (new) and the superseded 3.3/8.2/8.3 readings of `docs/porting/apple-silicon-verification.md`; **not touched:** the swapchain/`Present` sizing path (HiDPI slice) and the GUI callback paths | the retail main menu renders correctly: `CopyRects` between a host surface and an image of an emulated format converts representations instead of reinterpreting bytes, gated by two `zh-resource-lock-tests` cases (residual 0.000 against a CPU reference, wrong-image controls 117.2 and 65.9) that CI runs in both swizzle modes | (this slice) |
